public class SeguroDeVida implements Tributavel {
    // Outros atributos e métodos da classe

    @Override
    public double calcularTributos() {
        // Retorna a taxa fixa de 42 reais como tributos
        return 42.0;
    }
}